/// <reference types="react" />
import * as React from 'react';
export interface Props {
    url?: string;
    children?: React.ReactNode;
    external?: boolean;
    onClick?(): void;
}
export default function Link({url, children, onClick, external}: Props): JSX.Element;
