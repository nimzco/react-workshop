/// <reference types="react" />
import * as React from 'react';
export declare type Error = boolean | string;
export interface Props {
    id: string;
    label: string;
    error?: Error;
    labelHidden?: boolean;
    children?: string;
    helpText?: React.ReactNode;
}
export default function Choice({id, label, error, children, labelHidden, helpText}: Props): JSX.Element;
export declare function helpTextID(id: string): string;
export declare function errorID(id: string): string;
