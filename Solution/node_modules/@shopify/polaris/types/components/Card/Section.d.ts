/// <reference types="react" />
import * as React from 'react';
export interface Props {
    title?: string;
    children?: React.ReactNode;
    subdued?: boolean;
}
export default function Section({children, title, subdued}: Props): JSX.Element;
