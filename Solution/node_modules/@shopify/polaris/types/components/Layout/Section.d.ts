/// <reference types="react" />
import * as React from 'react';
export interface Props {
    children?: React.ReactNode;
    secondary?: boolean;
}
export default function Section({children, secondary}: Props): JSX.Element;
