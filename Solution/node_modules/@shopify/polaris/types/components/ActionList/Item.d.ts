/// <reference types="react" />
import { IconableAction } from '../../types';
export interface Props extends IconableAction {
    image?: string;
}
export default function Item({content, url, onAction, icon, image}: Props): JSX.Element;
