/// <reference types="react" />
import * as React from 'react';
import Panel from './Panel';
export interface TabDescriptor {
    id: string;
    url?: string;
    title: string;
    panelID?: string;
}
export interface Props {
    selected: number;
    tabs: TabDescriptor[];
    fitted?: boolean;
    children?: React.ReactNode;
    onSelect?(selectedTabIndex: number): void;
}
export interface State {
    disclosureWidth: number;
    tabWidths: number[];
    visibleTabs: number[];
    hiddenTabs: number[];
    containerWidth: number;
    showDisclosure: boolean;
    tabToFocus: number;
}
export default class Tabs extends React.PureComponent<Props, State> {
    static panel: typeof Panel;
    state: State;
    componentWillReceiveProps(nextProps: Props): void;
    render(): JSX.Element;
    private handleKeyPress(event);
    private renderTabMarkup(tab, index);
    private handleFocus(event);
    private handleBlur(event);
    private handleDisclosureActivatorClick();
    private handleClose();
    private handleMeasurement(measurements);
    private handleTabClick(tab);
}
