/// <reference types="react" />
export declare type Size = 'small' | 'medium' | 'large';
export interface Props {
    size?: Size;
    name?: string;
    initials?: string;
    customer?: boolean;
    source?: string;
    accessibilityLabel?: string;
}
export default function Avatar({name, source, initials, customer, size, accessibilityLabel}: Props): JSX.Element;
