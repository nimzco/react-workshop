/// <reference types="react" />
import * as React from 'react';
import { HSBColor, HSBAColor } from './types';
export interface State {
    pickerSize: number;
}
export interface Color extends HSBColor {
    alpha?: HSBAColor['alpha'];
}
export interface Props {
    color: Color;
    allowAlpha?: boolean;
    onChange(color: HSBAColor): void;
}
export default class ColorPicker extends React.PureComponent<Props, State> {
    state: State;
    private colorNode;
    componentDidMount(): void;
    render(): JSX.Element;
    private setColorNode(node);
    private handleHueChange(hue);
    private handleAlphaChange(alpha);
    private handleDraggerMove({x, y});
}
