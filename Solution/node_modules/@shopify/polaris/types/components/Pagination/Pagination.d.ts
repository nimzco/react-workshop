/// <reference types="react" />
export interface PaginationDescriptor {
    hasNext?: boolean;
    hasPrevious?: boolean;
    nextURL?: string;
    previousURL?: string;
    onNext?(): void;
    onPrevious?(): void;
}
export interface Props extends PaginationDescriptor {
    plain?: boolean;
}
export default function Pagination({hasNext, hasPrevious, nextURL, previousURL, onNext, onPrevious, plain}: Props): JSX.Element;
