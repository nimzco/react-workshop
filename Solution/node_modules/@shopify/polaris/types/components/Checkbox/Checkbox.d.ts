/// <reference types="react" />
import * as React from 'react';
import { Error } from '../Choice';
export interface Props {
    label: string;
    labelHidden?: boolean;
    checked?: boolean;
    helpText?: React.ReactNode;
    id?: string;
    name?: string;
    value?: string;
    error?: Error;
    disabled?: boolean;
    onChange?(newValue: boolean): void;
    onFocus?(): void;
    onBlur?(): void;
}
export default function Checkbox({id, label, labelHidden, helpText, checked, error, disabled, onChange, onFocus, onBlur, name, value}: Props): JSX.Element;
