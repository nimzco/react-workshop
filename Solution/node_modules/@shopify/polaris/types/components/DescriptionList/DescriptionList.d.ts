/// <reference types="react" />
import * as React from 'react';
export interface Item {
    term: string;
    description: React.ReactNode;
}
export interface Props {
    items: Item[];
}
export default function DescriptionList({items}: Props): JSX.Element;
