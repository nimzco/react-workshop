/// <reference types="react" />
import * as React from 'react';
import { PreferredPosition } from '../PositionedOverlay';
export declare enum CloseSource {
    Click = 0,
    EscapeKeypress = 1,
    FocusOut = 2,
    ScrollOut = 3,
}
export interface Props {
    id: string;
    active: boolean;
    preventAutofocus?: boolean;
    sectioned?: boolean;
    preferredPosition?: PreferredPosition;
    children?: React.ReactNode;
    activator: HTMLElement;
    onClose(source: CloseSource): void;
}
export default class PopoverOverlay extends React.PureComponent<Props, never> {
    private contentNode;
    componentDidUpdate({active: wasActive}: Props): void;
    render(): JSX.Element;
    private renderOverlay(transitionStatus);
    private renderPopover(transitionStatus, overlayDetails);
    private setContentNode(node);
    private handleClick(event);
    private handleScrollOut();
    private handleEscape();
    private handleFocusFirstItem();
    private handleFocusLastItem();
}
