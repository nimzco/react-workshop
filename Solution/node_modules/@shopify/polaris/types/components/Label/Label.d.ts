/// <reference types="react" />
import { Action } from '../../types';
export { Action };
export interface Props {
    children?: string;
    id: string;
    action?: Action;
    hidden?: boolean;
}
export declare function labelID(id: string): string;
export default function Label({children, id, action, hidden}: Props): JSX.Element;
