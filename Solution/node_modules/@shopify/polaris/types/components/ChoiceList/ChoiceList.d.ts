/// <reference types="react" />
export interface ChoiceDescriptor {
    value: string;
    label: string;
}
export declare type Choice = ChoiceDescriptor;
export interface Props {
    title?: string;
    titleHidden?: boolean;
    choices: Choice[];
    selected: string[];
    name?: string;
    allowMultiple?: boolean;
    onChange?(selected: string[]): void;
}
export default function ChoiceList({title, titleHidden, allowMultiple, choices, selected, onChange, name}: Props): JSX.Element;
