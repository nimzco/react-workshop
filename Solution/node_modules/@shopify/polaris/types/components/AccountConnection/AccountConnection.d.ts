/// <reference types="react" />
import * as React from 'react';
import { Action } from '../../types';
export interface Props {
    connected?: boolean;
    action?: Action;
    avatarUrl?: string;
    accountName?: string;
    title?: React.ReactNode;
    details?: React.ReactNode;
    termsOfService?: React.ReactNode;
}
export default function AccountConnection({connected, action, avatarUrl, accountName, title, details, termsOfService}: Props): JSX.Element;
