/// <reference types="react" />
import * as React from 'react';
import { Range, Months, Year } from '@shopify/javascript-utilities/dates';
export { Range, Months, Year };
export interface Props {
    selected?: Date | Range;
    month: Months;
    year: Year;
    disableDatesBefore?: Date;
    disableDatesAfter?: Date;
    multiMonth?: boolean;
    onChange?(date: Range): void;
    onMonthChange?(month: Months, year: Year): void;
}
export interface State {
    hoverDate?: Date;
    focusDate?: Date;
}
export default class DatePicker extends React.PureComponent<Props, State> {
    constructor(props: Props);
    render(): JSX.Element;
    private handleFocus(date);
    private handleKeyUp(event);
    private setFocusDateAndHandleMonthChange(date);
    private handleDateSelection(selected);
    private handleMonthChangeClick(month, year);
    private handleHover(date);
}
