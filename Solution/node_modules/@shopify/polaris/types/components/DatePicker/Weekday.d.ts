/// <reference types="react" />
import { Weekdays } from '@shopify/javascript-utilities/dates';
export interface Props {
    label: Weekdays;
    title: string;
}
export default function Weekday({label, title}: Props): JSX.Element;
