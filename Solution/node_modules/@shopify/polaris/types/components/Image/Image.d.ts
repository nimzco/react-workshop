/// <reference types="react" />
import * as React from 'react';
export interface SourceSet {
    source: string;
    descriptor?: string;
}
export interface Props extends React.HTMLProps<HTMLImageElement> {
    alt: string;
    source: string;
    sourceSet?: SourceSet[];
}
export default function Image({sourceSet, source, ...rest}: Props): JSX.Element;
