/// <reference types="react" />
export declare type Status = 'success' | 'info' | 'attention' | 'warning';
export declare type Progress = 'incomplete' | 'partiallyComplete' | 'complete';
export interface Props {
    children?: string;
    status?: Status;
    progress?: Progress;
}
export default function Badge({children, status, progress}: Props): JSX.Element;
