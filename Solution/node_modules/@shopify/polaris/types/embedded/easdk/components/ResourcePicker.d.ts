import Messenger from '../Messenger';
import Modal from './Modal';
export interface OpenOptions {
    title?: string;
    products?: boolean;
    collections?: boolean;
    allowMultiple?: boolean;
    onCancel?(): void;
    onSelection?(resources: object[]): void;
}
export default class ResourcePicker {
    private messenger;
    private modal;
    constructor(messenger: Messenger, modal: Modal);
    close(): void;
    open({title, products, collections, allowMultiple, onCancel, onSelection}: OpenOptions): void;
}
